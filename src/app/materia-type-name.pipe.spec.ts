import { MateriaTypeNamePipe } from './materia-type-name.pipe';

describe('MateriaTypeNamePipe', () => {
  it('create an instance', () => {
    const pipe = new MateriaTypeNamePipe();
    expect(pipe).toBeTruthy();
  });
});
